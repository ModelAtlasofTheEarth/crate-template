# rendered site (regenerated by CI / `crate render`)
_site/
# Crate-O writes this on save; the toolkit regenerates the page anyway
ro-crate-preview.html

.DS_Store
__pycache__/
# pixi's local env dir (the manifest+lock are tracked; the env itself is not)
.pixi/
